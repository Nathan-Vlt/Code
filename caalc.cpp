#include <iostream>
using namespace std;

int main()
{
    double nombre (5.3);
    nombre += (3.2);
    nombre -= (2);
    nombre *= (10);
    nombre /= (2);
    cout << nombre << endl;
return 0;
}

